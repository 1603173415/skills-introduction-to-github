# 如何提交Word文档进行学术编辑

## 问题说明

当前对话界面不支持直接上传或解析Word（.docx/.doc）格式文件。

## 推荐提交方式

### 方式一：将Word内容粘贴为纯文本

1. 打开您的Word文档
2. 全选内容（Ctrl+A）
3. 复制（Ctrl+C）
4. 直接粘贴到对话框中发送

### 方式二：将Word文档上传至本仓库

1. 在本仓库中新建一个文件夹，例如 `drafts/`
2. 将您的 `.docx` 文件上传至该文件夹
3. 在对话中提供文件的GitHub链接
4. 编辑助手将读取文件内容并进行处理

### 方式三：导出为纯文本或Markdown

1. 在Word中选择"另存为"
2. 选择格式为"纯文本（.txt）"或手动整理为Markdown格式
3. 将文本内容粘贴至对话框

## 注意事项

- 公式建议使用LaTeX格式表达，例如：`$G \cdot R = \text{const}$`
- 图表说明文字可直接粘贴，图片本身暂无法解析
- 每次建议提交一个章节，以便进行精准的逻辑审计
